import 'package:flutter/material.dart';

class AppColors {
static const Color primary = Color(0xFF1B5E20);      // Xanh lá đậm (Sidebar)
  static const Color accent = Color(0xFF4CAF50);       // Xanh lá nổi bật (Tab đang chọn)
  static const Color background = Color(0xFFF8FAFC);   // Nền xám rất nhẹ sáng
  static const Color textDark = Color(0xFF0F172A);     // Chữ tối đậm nét
}
